# SolaraX pipeline dependencies
#
#   pip install -r pipeline/requirements.txt
#
# Four, and three of them cover ingestion alone. Everything else the pipeline
# uses is Python standard library (csv, json, datetime, concurrent.futures, argparse, re).
#
# Verified working on Python 3.14.3 at the versions noted below. Minimums are
# floors rather than exact pins so a teammate on a slightly different Python can
# still install — if you hit a version-specific problem, pin exactly and say so
# here rather than leaving the next person to rediscover it.

# S3 access to the public PVDAQ bucket. Used with UNSIGNED signature config —
# no AWS credentials are needed or wanted.
boto3>=1.34            # tested: 1.43.72

# Dataframes: the metrics join, the long->wide pivot, daily aggregation.
pandas>=2.2            # tested: 2.3.3

# Parquet engine behind pandas.read_parquet / to_parquet. PVDAQ ships parquet
# and every intermediate in data/processed/ is parquet.
pyarrow>=15.0          # tested: 23.0.1

# Solar position, irradiance decomposition and transposition, cell temperature
# and the PVWatts DC model — the M2 baseline chain in pipeline/baseline.py.
#
# NEEDED ONLY FOR M2. pipeline/detect_cohort.py (M3, the detector every ringgit
# figure depends on) is stdlib-only and imports nothing from here. The pipeline
# generates a complete, valid dispatch.json with pvlib absent — expected_kwh is
# simply null and irradiance_source reads NONE, both of which the schema
# supports. Install it to get the cross-check, not to get a working product.
pvlib>=0.13            # tested: 0.15.2

# --- NOT pinned here, on purpose --------------------------------------------
# scikit-learn is still unused. The detector is median/MAD arithmetic on the
# standard library, which needs no ML stack. Add it only when something
# actually imports it.
