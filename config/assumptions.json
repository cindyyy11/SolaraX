{
  "_comment": "Every commercial constant lives here. No magic numbers in code, ever. This whole block is copied verbatim into dispatch.json.assumptions so Screen 4 needs no second fetch — see docs/Schema.md section 4.",
  "tariff_rm_per_kwh": 0.4373,
  "assumed_yield_kwh_per_kwp_day": 3.8,
  "cost_per_visit_rm": 1500,
  "dispatch_threshold_rm_per_month": 1500,
  "min_cohort_size": 5,
  "baseline_visit_frequency_per_year": 12,
  "sub_site_deviation_threshold": -0.15,
  "min_plausible_performance_index": 2.0,
  "same_trip_radius_km": 2.0,
  "projection_horizon_months": 12,
  "soiling_rate_per_day": 0.0047,
  "soiling_max_loss_fraction": 0.6,
  "malaysia_reference_yield_kwh_per_kwp_day": 3.656,
  "tariff_rm_per_kwh_range": {
    "low": 0.3649,
    "high": 0.492
  },
  "malaysia_reference_yield_kwh_per_kwp_day_range": {
    "low": 3.523,
    "high": 3.901
  },
  "cost_per_visit_rm_range": {
    "low": 800,
    "high": 2500
  },
  "co2e_grid_factor_kg_per_kwh": 0.74,
  "tier": "Tier 2 — labelled assumption, see PRD v2 section 6",
  "notes": {
    "tariff_rm_per_kwh": "Malaysian RP4, in force 1 Jul 2025 to 31 Dec 2027. Base tariff 45.40 sen/kWh is TIER 1 - set by Suruhanjaya Tenaga (the regulator), revised down from the 45.62 sen approved in Dec 2024. Source: https://www.st.gov.my/jadual-elektrik-baharu-lebih-236-juta-pengguna-domestik-semenanjung-nikmati-kadar-lebih-adil The second half is the Automatic Fuel Adjustment, and it is NOT a component of the tariff: ST sets it MONTHLY, it is signed, and it has been negative in 9 of the 14 months since RP4 began (range -8.91 to +3.80 sen/kWh). This figure uses the 14-month mean of -1.67, so 45.40 - 1.67 = 43.73 sen = RM 0.4373. The mean is the right basis because the product is a recurring monthly service, not a single billing month. Full series in docs/RESEARCH.md section 4.",
    "tariff_rm_per_kwh_range": "Base 45.40 sen across the full OBSERVED AFA swing: -8.91 sen (Nov 2025, the lowest on record) to +3.80 sen (Aug 2026, the highest). Every endpoint is a rate ST actually published, so this range is evidence rather than a guess. Screen 4's pessimistic toggle takes the low end, where each recovered kWh is worth least - the dispatch conclusion has to survive there. Replaces an earlier guessed band of 0.4359 to 0.5259, whose upper end (52.59 sen) has never occurred under RP4.",
    "cost_per_visit_rm": "Tier 2 assumption. No public figure exists for Malaysian C&I site-visit cost. Mid point of an 800 to 2500 range covering technician time, travel and mobilisation. Costed per TRIP, not per site — see same_trip_radius_km.",
    "same_trip_radius_km": "Tier 2 assumption. Sites within this distance of each other are reached in one mobilisation, so they cost one visit between them, not one each. Five of our sites (the Agassi buildings) share byte-identical coordinates and two more pairs sit within 300 m, so counting saved visits per site overstates the saving by more than half. A trip counts as avoided only when NO site in the group is being dispatched: if a technician is already going to that address, skipping the neighbours saves nothing.",
    "projection_horizon_months": "Horizon for Screen 4's 'if this month repeats' figure. A presentation assumption, not a measurement: the pipeline observes ONE month, so anything annual is a straight-line projection and is labelled as one. Lives here rather than in code because it moves a ringgit figure on screen — CLAUDE.md, no magic numbers.",
    "soiling_rate_per_day": "Soiling loss accrued per day with no cleaning, used ONLY by pipeline/fault_injection.py to shape a synthetic fault. 0.47 percent/day = 10.2 percent/month. Source: Malaysian field study, docs/RESEARCH.md section 3. Never applied to real generation figures - this is the magnitude of a fault we inject on purpose so M3 has ground truth.",
    "soiling_max_loss_fraction": "Floor on the soiling factor. Unbounded, 0.47 percent/day reaches total loss on day 213 and goes negative inside our 233-day window. 0.60 is the top of the observed Malaysian range (reductions measured up to 58.67 percent, docs/RESEARCH.md section 3); beyond it a site is effectively offline and the story is an outage, not soiling.",
    "cost_per_visit_rm_range": "Screen 4's pessimistic toggle recomputes headline figures at the unfavourable end of every range: lowest tariff, highest visit cost.",
    "dispatch_threshold_rm_per_month": "A site is worth visiting when the monthly loss exceeds the cost of going, so this tracks cost_per_visit_rm exactly: RM 1500, the MID point of the 800 to 2500 range. An earlier version of this note claimed the high end of that range, which the value has never matched - the high end is 2500. The distinction matters because this constant alone decides whether a site appears under Dispatch or Monitor, so the number and its justification have to agree. The unfavourable corner is not baked in here; Screen 4's pessimistic toggle is where it is tested, at the lowest tariff and the highest visit cost.",
    "min_cohort_size": "Below this, the cohort control group is too weak to be meaningful. PRD v2 section 15. Cohorts under this size are flagged, never hidden.",
    "sub_site_deviation_threshold": "An inverter is flagged when its mean daily output sits this far below the median of its siblings on the same roof. Siblings share weather perfectly, so divergence is unambiguous. Ratio-based because PVDAQ does not publish per-inverter capacity.",
    "min_plausible_performance_index": "Data-quality floor in kWh/kWp/day. A working C&I site does not average below this anywhere the fleet operates; a site that does has incomplete telemetry, not a fault. Such sites are excluded from detection AND from their cohort median, because a permanently depressed reading both generates a false dispatch and drags the peer baseline down. Exclusion is stated on screen, never silent.",
    "assumed_yield_kwh_per_kwp_day": "Nominal specific yield used only for sanity checks and demo estimates, never as a substitute for the M2 physics baseline. NOT a Malaysian figure and not derived from one — see malaysia_reference_yield_kwh_per_kwp_day, which is a separate constant for a separate purpose. Do not merge them.",
    "malaysia_reference_yield_kwh_per_kwp_day": "DERIVED REFERENCE ESTIMATE, not a measurement and not Tier 1. Mean modelled specific yield across four real Malaysian coordinates (Bukit Raja/Klang, Senai, Nilai, Ipoh), from the PVGIS-ERA5 meteorological reanalysis (ECMWF ERA5) averaged 2005-2020 through PVGIS's physical PV model. ERA5 assimilates satellite and ground observations but is not itself a satellite dataset. Depends entirely on chosen parameters: 100 kWp c-Si, roof-mounted (building-integrated), 10 degree tilt, 0 degree azimuth (south), 14 percent system loss. Change any one and the figure moves — roof-mounting alone costs about 4 percent against free-standing. Regenerate with pipeline/fetch_malaysia_reference.py; method note in docs/MALAYSIA-REFERENCE.md. NO Malaysian site measured this: no openly reusable per-site Malaysian inverter time series exists (docs/RESEARCH.md section 5). Never present it as measured output and never use it as ground truth for detection accuracy.",
    "malaysia_reference_yield_kwh_per_kwp_day_range": "Spread across the four sites, lowest (Senai) to highest (Bukit Raja/Klang). This range falls entirely inside the observed spread of the real PVDAQ fleet (3.52 to 4.76 kWh/kWp/day, recomputable with pipeline/fleet_median.py), which supports using that fleet to demonstrate the method. It does NOT claim the fleets are operationally equivalent — one side is measured output from specific hardware and operations, the other is modelled reference output.",
    "baseline_visit_frequency_per_year": "Calendar-driven visit cadence the product replaces. Twelve equals monthly.",
    "co2e_grid_factor_kg_per_kwh": "Tier 1. Malaysia grid emission factor, Energy Commission 2024. Generation at risk times this factor gives the tCO2e recoverable if the flagged faults are fixed, for the ESG rubric row. Recoverable, not avoided: nothing has been repaired yet."
  }
}
